<!--pages/paiming/paiming.wxml-->
<view class="page">
  <view class="page__bd">
    <!-- <view class="weui-cells__title">带图标、说明、跳转的列表项</view> -->
    <view style="background-image: url('../../resources/img/paiming.png');width:100%;height:170px">
      <!-- <image src="../../resources/img/paiming.png" style="width:100%;height:170px"></image> -->
      <!-- <view class="" style="margin:0 auto">
         <image src="{{firstHeadUri}}" style="margin-right: 5px;vertical-align: middle;width:20px; height: 20px;"> 
        <image src="{{firstHeadUri}}">
        </image>
      </view> -->
      <view class="weui-cell__bd">{{firstNickname}}</view>
      <view class="page__bd page__bd_spacing">
      <view class="button-sp-area">
            <button class="weui-btn mini-btn" type="default" size="mini" bindtap='pause'>按钮</button>
        </view>
      </view>
    </view>
    <!-- <view style='margin-top:0' class="weui-cells weui-cells_after-title" wx:for="{{items}}" wx:for-index="index" wx:for-item="item">
      <navigator class="weui-cell weui-cell_access" hover-class="weui-cell_active">
        <view>{{index+1}}</view>
        <view class="weui-cell__hd">
          <image src="{{item.user.headUri}}" style="margin-right: 5px;vertical-align: middle;width:20px; height: 20px;" alt="image">
          </image>
        </view>
        <view class="weui-cell__bd">{{item.user.nickname}}</view>
        <view class="weui-cell__ft ">{{item.rememberCount}}</view>
      </navigator>
    </view> -->


    <view class="weui-cells weui-cells_after-title" wx:for="{{items}}" wx:key="ming" wx:for-index="index" wx:for-item="item">
      <view class="weui-cell" hover-class="weui-cell_active">
        <view>{{index+1}}</view>
        <view class="weui-cell__hd">
          <image src="{{item.user.headUri}}" style="margin-right: 5px;vertical-align: middle;width:20px; height: 20px;"></image>
        </view>
        <view class="weui-cell__bd">{{item.user.nickname}}</view>
        <view class="weui-cell__ft ">{{item.rememberCount}}</view>
      </view>
    </view>
  </view>
</view>